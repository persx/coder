# Global (bot) runtime; secrets should go into GitHub Actions → Secrets
ANTHROPIC_API_KEY=
GH_TOKEN=

# Target selection (can be set via `targets/*.env` and exported before run)
TARGET_REPO_URL=https://github.com/persx/persx.git
TARGET_OWNER_REPO=persx/persx
PR_BASE=main
TASK_FILE=tasks/persx.yaml
SAFEOPS=configs/safeops_persx.json
CLAUDE_MODEL=claude-3-5-sonnet-latest
MAX_LOOPS=10
